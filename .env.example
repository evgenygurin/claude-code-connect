# Claude Code + Linear Integration Configuration

# Linear API Configuration
# -------------------------
# Option 1: Direct API Token (simpler, but less secure)
LINEAR_API_TOKEN=lin_api_your_token_here
LINEAR_ORGANIZATION_ID=your-organization-id

# Option 2: OAuth App (recommended for production)
# Register your app at https://linear.app/settings/api/applications
ENABLE_OAUTH=true
LINEAR_CLIENT_ID=your-linear-client-id
LINEAR_CLIENT_SECRET=your-linear-client-secret
OAUTH_REDIRECT_URI=http://localhost:3005/oauth/callback

# GitHub PR Comment Agent Configuration
# ---------------------------------------
# Required: GitHub personal access token with repo scope
GITHUB_TOKEN=ghp_your_github_token_here

# Optional: GitHub webhook secret for signature validation
GITHUB_WEBHOOK_SECRET=your-github-webhook-secret

# Optional: Default repository owner/org (can be overridden by webhook)
GITHUB_OWNER=your-github-username-or-org

# Optional: Default repository name (can be overridden by webhook)
GITHUB_REPO=your-repo-name

# Required: Path to your project directory
PROJECT_ROOT_DIR=/path/to/your/project

# Optional: Claude agent user ID (auto-detected if not set)
# CLAUDE_AGENT_USER_ID=your-user-id

# Optional: Webhook secret for Linear webhook verification
# LINEAR_WEBHOOK_SECRET=your-webhook-secret

# Optional: Git branch configuration
DEFAULT_BRANCH=main
CREATE_BRANCHES=true

# Optional: Server configuration
# Port 3005 for local development, 3000 for Codegen Web Preview (auto-detected)
WEBHOOK_PORT=3005
SESSION_TIMEOUT_MINUTES=30

# Optional: Claude executable path (if not in PATH)
# CLAUDE_EXECUTABLE_PATH=/usr/local/bin/claude

# Optional: Debug logging
DEBUG=false

# Boss Agent Configuration (Advanced)
# -------------------------------------
# Enable Boss Agent mode for complex task delegation to Codegen
ENABLE_BOSS_AGENT=false

# Complexity threshold (1-10) - tasks with complexity >= threshold will be delegated
BOSS_AGENT_THRESHOLD=6

# Maximum number of concurrent sub-agents
MAX_CONCURRENT_AGENTS=3

# Codegen Integration (Required when ENABLE_BOSS_AGENT=true)
# -----------------------------------------------------------
# Get your API token from: https://codegen.com/token
# Get your organization ID from: https://codegen.com/settings

# Required: Codegen API token for task delegation
CODEGEN_API_TOKEN=your_codegen_api_token_here

# Required: Codegen organization ID
CODEGEN_ORG_ID=your_organization_id_here

# Optional: Codegen API base URL (defaults to https://api.codegen.com)
CODEGEN_BASE_URL=https://api.codegen.com

# Optional: Default timeout for Codegen tasks in milliseconds (defaults to 3600000 = 1 hour)
CODEGEN_DEFAULT_TIMEOUT=3600000

# Optional: Enable Codegen webhook callbacks for progress updates
CODEGEN_WEBHOOK_ENABLED=false

# Optional: Codegen webhook secret for signature validation
CODEGEN_WEBHOOK_SECRET=your-codegen-webhook-secret

# Optional: Auto-merge PRs from Codegen (requires review approval)
CODEGEN_AUTO_MERGE=false

# Optional: Require code review for Codegen PRs (recommended)
CODEGEN_REQUIRE_REVIEW=true

# Optional: Default reviewers for Codegen PRs (comma-separated GitHub usernames)
CODEGEN_DEFAULT_REVIEWERS=reviewer1,reviewer2

# Mem0 Integration Configuration
# --------------------------------
# Get your API key from: https://app.mem0.ai/
# Mem0 provides persistent memory across Claude sessions

# Required: Mem0 API key
MEM0_API_KEY=your_mem0_api_key_here

# Optional: Enable Mem0 integration
MEM0_ENABLED=true

# Optional: Enable verbose Mem0 logging
MEM0_VERBOSE_LOGGING=false
